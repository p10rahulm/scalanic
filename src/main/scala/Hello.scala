/*
 *   Our first program in scala. Here we call the program by
 *   sbt:Scala> run Hello
 *   which gives the required output
 *
 *   The other way is as usual from the scala console like so
 *   scala> Hello.main(Array())
 *   Hello World!
 *
 *
 *   The last method is to press the play button next to the class in the code below
 *
 */
object Hello {
  def main(args: Array[String]) = {
    println("Hello World!")
  }

}
